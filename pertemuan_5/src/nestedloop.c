#include <stdio.h>

int main()
{
    int x, y;
    for ( x = 0; x <= 5; x++)
    {
        for ( y = 0; y <= 3; y++)
        {
            printf("Perulangan ke (%d, %d)\n", x, y);
        }
        
    }
    
}